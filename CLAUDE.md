# 생각정리 도구함 — 프로젝트 메모리

## 소유자와 원칙 (승인된 기억)

- 소유자: 최진이 교수 (chlwlsdl@gmail.com). 기본 언어는 한국어, 정중한 존댓말.
- 이 앱의 콘텐츠는 **최진이 교수의 생각정리 강의**를 바탕으로 한다. 특정 도서·저자 출처 표기는 넣지 않는다. (만다라트·로직트리 등 일반 기법 명칭은 사용 가능)
- 운영 원칙: **완전 무료 운영** — 정적 호스팅(GitHub Pages) + 사용자 데이터는 각자 구글 드라이브 + AI는 BYOK(각자 자기 API 키). 운영자 비용이 생기는 구조 변경은 소유자 확인 후에만.
- 자기학습 모드: **자동 기억(승인형 아님)** — 사소하고 안전한 선호·사실은 제안 절차 없이 아래 학습 로그에 바로 적층한다. 민감한 내용(건강·심리·사적 관계 등)은 기록하지 않는다.

## 배포 정보

- 공개 주소: https://choijinyi.github.io/Ezer-jin/ (저장소 이름이 대문자 E라 소문자 주소는 404)
- 저장소: choijinyi/Ezer-jin, 작업 브랜치: `claude/lucid-volta-t8pgj6` (Pages가 이 브랜치 루트에서 배포. 푸시하면 1~2분 내 자동 반영)
- 구글 OAuth: GCP 프로젝트 "AI Thinking"(ai-thinking-508407), 클라이언트 ID는 `config.js`에 있음(공개값). 동의 화면은 프로덕션 게시 완료, 권한은 비민감 범위만(drive.file, openid, email, profile).

## 코드 구조

- **`index.html`이 단일 소스**다. 앱 전체(도구 10종 + 대시보드 + 구글 드라이브 동기화 + AI BYOK)가 한 파일에 있고, `config.js`(OAuth 클라이언트 ID), `guide.html`(사용 안내), `privacy.html`(개인정보처리방침), `README.md`(운영 절차)가 곁에 있다.
- 사용자 데이터: 로그인 시 드라이브의 "생각정리 도구함 데이터.json"(drive.file 권한, 원격 우선 동기화, 1.5초 디바운스 업로드), 미로그인 시 localStorage(`thinkTools.v1`, version 3 스키마 + 방어적 초기화).
- AI 키는 localStorage(`thinkTools.ai`)에만 저장하며 **드라이브 동기화 파일에 절대 넣지 않는다.**
- 삭제 UI는 브라우저 confirm() 대신 "두 번 눌러 확정"(arm 함수) 방식 — 샌드박스 환경에서 모달이 차단되기 때문.
- 도구 이름: 만다라트는 사용자 표기상 **"생각 나무 계획표"**로 부른다(내부 라우트/변수는 mandal 유지).

## 하네스 학습 로그

- 2026-09-12: 도구명 변경 — "만다라트" → "생각 나무 계획표"(기법 병기), 대시보드 최상단에 "오늘 해야 할 일"(어제 일기의 '내일 가장 중요한 일' 자동 승계 + 직접 추가).
- 2026-09-12: 도서 출처 표기·고유 인용 전면 제거, 최진이 교수 강의 기반으로 명시.
- 2026-09-12: 저장 방식을 drive.appdata → drive.file로 전환(심사 없는 전체 공개를 위해).
- 2026-09-12: AI는 방향 A(질문·제안형 조력자)로 확정 — AI는 답이 아니라 질문·분류안만 제안, 채택은 사용자. 퀘스천맵 질문 제안·브레인스토밍 분류 제안 구현(BYOK, 기본 추천 Gemini).
- 2026-09-12: 넓은 화면에서 안내문 폭 제한 제거. 하단에 사용 안내·개인정보처리방침 링크.
- 2026-09-12: 자동 기억 모드 활성(소유자 승인).
